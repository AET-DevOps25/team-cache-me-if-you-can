// TODO: implement a page of Materials which allowed users to check all uploaded materials and
// submit materials
export function Material() {
  //TODO: submit materials
  //const handleSubmit = async () => {
  //   const response = await fetch('/api/material/submit', {
  //   method: 'POST',
  //   body: JSON.stringify(formData)
  // });
  // return response.ok;
  // };

  //TODO: get mateirals
  //const getMaterial = async () => {
  //  const response = await fetch(`/api/material/${id}`);
  //  const result = await response.json();};

  return <></>;
}
