//TODO: implement a page to show the information of the study group
export function Create() {
  //TODO: get group info from api
  //const getGroupInfo = async () => {
  //  const response = await fetch(`/api/group/info/${id}`);
  //  const result = await response.json();};
  return <></>;
}
