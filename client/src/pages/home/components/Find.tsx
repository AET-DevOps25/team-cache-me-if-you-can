//TODO: implement a page to find study group
export function Find() {
  //TODO: find the group
  //const findGroup = async () => {
  //  const response = await fetch(`/api/group/find/${name}`);
  //  const result = await response.json();};
  return <></>;
}
